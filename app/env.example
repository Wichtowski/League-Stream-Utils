# Database
MONGODB_URI=mongodb://localhost:27017/your-database-name

# JWT Configuration (CRITICAL: Generate a strong 32+ character secret)
JWT_SECRET=your-super-strong-jwt-secret-at-least-32-characters-long

# Admin Credentials (CRITICAL: Use strong credentials)
ADMIN_USERNAME=your-admin-username
ADMIN_PASSWORD=your-strong-admin-password-min-12-chars
ADMIN_EMAIL=admin@yourdomain.com

# Application
NEXTAUTH_URL=http://localhost:3000
NODE_ENV=development

# Riot API (optional)
RIOT_API_KEY=your-riot-api-key

# Security Notes:
# - JWT_SECRET: Use a cryptographically secure random string of at least 32 characters
# - ADMIN_PASSWORD: Must be at least 12 characters with uppercase, lowercase, numbers, and special characters
# - Never commit real secrets to version control
# - Use different secrets for development and production 